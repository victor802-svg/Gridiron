.venv/
__pycache__/
*.pyc
*.db
*.db-journal
*.db-wal
*.db-shm
.pytest_cache/
build/
dist/
*.spec.bak
.gridiron/
var/

# The access token lives here. Never committed: a secret in git history is a
# secret forever, because removing it later does not remove it from every
# clone and every reflog.
.env
dist/
build/
