# Brief — GRIDIRON_REPAIR (rulings on THE READ, 2026-09-23)

Saved before execution, as the standing contract requires. The operator's text
is unedited.

---

Rulings on THE READ, 2026-09-23. Jobs 2–5 are suspended. One job replaces them: GRIDIRON_REPAIR, class (a) defects only, each its own commit with a planting, in this order. No new features, no ranker or factor changes beyond what is named.

1. Closing line. The near-start pass reads every open recommendation on every run inside its window until the game starts; the last read before kickoff is the close. Retire the once-then-exclude at tasks.py:544 by name. Recompute CLV for every recommendation whose true pre-kickoff quote exists in the record; mark the 49 rows whose "close" was the recommendation price as unmeasured, never as zero. Planting: a recommendation with two near-start reads must close on the later one.

2. fs5. State whether fs5 can be trained on the live record today, and train it if so; if not, revert NFL and NCAAF spread and moneyline to the last trained set, dated, with the reason. Either way: run.py:99 may never skip an untrained market silently — a predict run with a skipped market fails by name, and the day strip shows it. Planting: an untrained market in the active set fails the run.

3. Corrections reach recommendations. recommend.py:324 reads the corrected probability, never the raw claim. Re-derive nothing retroactively (LAW 3); from the fix forward, every recommendation carries the correction that was current. Planting: a fitted correction that would flip a side must flip it.

4. Return-on-stake denominator: a no-side edge divides by the no-side cost. Re-grade the three recommendations it let through as "would not have cleared". Planting.

5. One recommendation per game and market, and never both sides. Recs 45 and 46 are the planting.

6. The at-the-line scorecard never pools forecasters or duplicates; per-forecaster, per-distinct-bet counts only, LAW 4 and LAW 6. Re-state every gate distance on the corrected counts.

7. Jobs: SlateAlreadyAnswered is a noop, not a failure; the run_task closing UPDATE moves inside the try and a hung "running" row older than its task's silent_after_hours is marked abandoned; every Gridiron-* task gains WakeToRun. Report awake fraction since the fix in each close-out.

8. READINESS: two BROKEN findings recorded; the observation window restarts on the date item 1 ships; the first clean CLV read is 21 days after that, not before.

Then the read again, on the repaired record, with the same seven measurers and verifiers, before any of jobs 2–5 are reconsidered. The one paragraph at the end says which market, if any, is worth the operator's money on the evidence — and if the answer is none, it says none.

---

## How this brief is read

**Jobs 2–5 of `2026-09-23-five-jobs.md` are suspended**, not cancelled. They
stay recorded there verbatim.

**Job 1's close-out commits first.** It is the read these rulings answer, and
it was mid-gate when they arrived. The gate failed on two tests that read the
live record and assumed its shape on 10 September. Those are repaired in their
own commit before anything else lands, because nothing may be committed on a
red gate.

**Every item writes, and every write that touches the live record is named.**
Item 1 recomputes closes and marks 49 rows unmeasured. Item 2 may train a
model. Item 4 re-grades three recommendations. The record's append-only rules
(LAW 3) govern how each is written. Where a rule would have to be stretched to
make the write, that is put to the operator as two readings, per "Who reads a
law".
