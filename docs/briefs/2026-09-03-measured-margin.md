Operator rulings, 2026-09-03, following the asked-line close-out.
Recorded verbatim.

1. cfb_expected_margin: adopt the MEASURED slope and intercept
   (0.9351 / 4.848), dated, under the measured-constant law — the
   old 1.0 / 9.79 goes in the comment as "assumed, did not
   reproduce". Retrain CFB; Saturday's rows stand; report the base
   rate before/after (expect 0.371 → near 0.50).
2. Plant the LAW 6 training-set break: every sport's training loader
   asserts a single sport in its rows; a loader returning two sports
   fails by name. Plant the already_answered gap: a sport adapter
   missing markets() fails at import, not at first use.
3. Docstrings that name an audit function (audit.<name>) must
   resolve — an AST check, planted with a phantom reference. A
   comment may not promise a mechanism that isn't there.
Then Session C (count markets) in a fresh session.
