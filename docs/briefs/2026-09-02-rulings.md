Rulings on the overnight close-out:

1. SCHEDULE: predict:nfl runs at 11:00 (later = more lines posted);
   update the stored setting to match the OS, with a history entry.
   INSTALL predict:cfb — Friday 09:00 — through the installer, and
   verify the OS holds it. College football has been predicted by
   hand; that ends today. Settings must show both agreeing.
2. MLB spread_line SIGN: recompute every stored row from the raw
   payload's favorite/underdog flags where the raw is retained;
   where it is not, mark the row's sign as unknown rather than
   guess. Planting: a run-line row whose sign contradicts its
   moneyline favourite fails.
3. MARGIN SD: the number is what the data says. Replace 4.71 with
   the 4.534 measured on 9,373 games, dated; keep the old entry in
   the comment with a note that it did not reproduce.
4. NFL DOUBLE SLATE: superseded rows are excluded from calibration
   and every scorecard — the latest before kickoff is the standing
   forecast (the precedent already applied to Picks). Never deleted.
   Guard: predict refuses to write a second full set for a slate it
   already answered, unless a factor-set version changed.
5. The six absent-operator rulings stand, wordmark included.

Then STEP 3 BUILD in its own session, against the probe: run line
at the market's ±1.5 with ESPN's explicit side labels, totals from
the model's own run expectation, the measured total SD under the
undated-SD guard, checklist ticked, live on the next slate. Then
STEP 4 (the CFB ladder). /closeout each, push.
