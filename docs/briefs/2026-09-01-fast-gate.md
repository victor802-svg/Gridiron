Make the gate fast and visible. Measure the suite: which tests cost
what. Then either xdist (audit the browser fixtures for shared
state first — server port, Chromium profile, the live DB) or a
declared two-tier split where the skipped tier is NAMED in the
output every run. Target: the full gate under 8 minutes, progress
streamed while it runs (no capture_output), and a test that the
gate refuses to report PASS if any tier was skipped silently.
