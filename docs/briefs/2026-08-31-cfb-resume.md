# Rulings on the calibration close-out, and CFB resumed

Pasted verbatim, 2026-08-31.

---

Rulings on the calibration close-out: thresholds stay as measured
(fit at 50, apply ~200); every audit scanner gets a known-positive
fixture it must match or fail at import (the backspace lineage, 4th
instance); the by-looking ratio is noted, not fixed.

Resume CFB: docs/briefs/2026-08-31-cfb.md, from B1's unmeasured
items (line coverage for spread/ML/total — the R-A slate size;
single-source identity; measured margin and total SDs; props
evidence). Two probe findings bind the design: the slate is the
UNION of 136 per-team schedules, never the weekly endpoint; venue
coordinates come from Open-Meteo's geocoder WITH the state filter,
dated and cached. Then B2-B6 as briefed, unattended contract in
force, push each phase, /closeout. The first forward slate is
Saturday.

---

## One term I could not look up

**"Unattended contract in force"** does not appear in CLAUDE.md,
docs/MENTOR.md, or any earlier brief or close-out in this repository —
the only occurrence of the word "unattended" anywhere is a K0–K4
close-out line about the scheduler firing on its own.

Read here as: **work through all six phases without stopping for
answers.** Make the judgment calls, record each one where it is made and
again in the close-out, and never wait on a reply. That is how this
session has already been running, so nothing changes; it is written down
because guessing at a named contract without saying so is exactly the
kind of silent assumption this project's rules exist to prevent.

If it means something more specific, the close-out's attention list is
where to correct it.
