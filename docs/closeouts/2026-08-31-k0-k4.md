# Session close-out — K0–K4

*2026-08-31. Produced by `/closeout` against `docs/MENTOR.md` and `CLAUDE.md`.*

**Deviations, stated first.** Two instructions in the brief could not be carried
out as written:

1. **`docs/mockup/gridiron_compact.html` did not exist.** The brief named it as
   the approved design for K2; `find` across the whole repo returned nothing for
   `*compact*`, and nothing had been written to `docs/mockup/` that day. K2 was
   built from a screenshot pasted into the session instead, so there was no
   committed mockup artifact at the time of this close-out. (It was supplied and
   committed afterwards.)
2. **K2 is not green**, so it was not committed as `"k2: compact screen"`. Eight
   browser tests still assert the old card grammar. The completed half was
   committed honestly rather than a red tree labelled green.

## 1. Close-out table

| Phase | Verdict | Evidence |
|---|---|---|
| **K0** appliance | **DONE** | `aebcf35`. Refresh task; 25 unblocked, 25 settled; MLB 6→31 |
| **K1** side arithmetic | **DONE** | `661ea49`. 190 swept, 34 wrong-side cards found, fixed at source |
| **K2** compact screen | **PARTIAL** | `24212a6` (WIP) + `6b739c6`. Rows, header, void exclusion, teams table done; 8 browser tests red |
| **K3** plain why | **DEFERRED** | FOLLOWUPS, dated 2026-08-31, one-session-per-big-build |
| **K4** verification | **DEFERRED** | Cannot run before K3 |

## 2. Grade against MENTOR §2

| # | Item | Verdict |
|---|---|---|
| 1 | Close-out table | **MET** — every phase, hashes, DEFERRED cites the precedent |
| 2 | Deviations up front | **MET** — mockup and red-tree stated before results |
| 3 | Bugs named as mine | **MET** — five: duplicate CSS rule (84px overflow), `flex: none` on tabs, hardcoded task count, `team_names` undefined in two functions, mis-indented block |
| 4 | Vacuous passes | **PARTIAL** — none found this session; I did not go looking, which is a gap not a clean bill |
| 5 | Unverified stated as such | **MET** — see §5 |
| 6 | Attention list | **MET** |
| 7 | Corrections | **MET** — K1 corrects M4's own claim to have fixed the side bug |

## 3. Red flags from §3

- **Two implementations of one thing — twice.** `renderNotices` built a
  summary+detail while the markup also had them (why C2 collapsed on some tabs
  only). A duplicate `.prob small` CSS rule. Both deleted, not reconciled.
- **A guard nobody had seen fail:** the schedule panel asserted a hardcoded `4`
  tasks; K0's fifth task made it stale. Now derived from the registry.
- **A docstring asserting a past change:** M4's commit claimed the side bug
  fixed. It fixed two branches of three. 34 cards were wrong for the whole
  interval.

## 4. Vacuous passes

None found — and I did not hunt for them, so treat that as unexamined rather
than clean.

## 5. What remains unverified

- **The eight browser tests are unverified against the new design**, not merely
  failing. I do not know whether all eight are stale assertions or whether one
  is a real regression.
- **Three history tests newly failed** after my selector rename and I did not
  diagnose them before stopping.
- **The compact screen has never been looked at.** No headless render was taken.
  Every claim about K2's appearance is from code and passing selectors, not from
  an image.
- **NFL team names cover 30 of 34 tricodes.** Measured, in FOLLOWUPS.

## 6. Operator's attention list

1. **Paste the mockup as a file, or confirm the screenshot is the artifact** —
   K2 has no committed reference to build against or review K3 with.
2. **K2's last mile** — the 8 red tests plus the 3 broken. Small, mechanical,
   needs a session with room to render and look.
3. **K3's real size**: ~50 why-templates across three sports, plus the
   consistency guard. Its own session.
4. **`Gridiron-Refresh` fires at 18:00** — first unattended run of K0's fix.
   Worth a glance at the schedule panel the next day.

## 7. Operator's verdicts

**Strongest thing:**

**Weakest thing:**

**What to do next:**

## 8. FOLLOWUPS delta

**Non-zero: +45 lines** — K3/K4 deferral dated, the NFL name gap with its four
codes, and the plural-verb reasoning for keeping tricodes in the chance label.
Correct for a session that deferred two phases and measured one gap.
