# Session close-out — GRIDIRON_16 (W1–W4)

Brief: `docs/briefs/2026-09-02-overnight.md` (saved verbatim first, per
MENTOR §4a; paste verified complete).

Commits: `775d9f7` the `.env` fix · `6dc5252` the three picks defects ·
`b7fd7cb` the brief · `c31c9fd` W1 · `948e094` W2 · `bd7ac2f` W3 ·
`67b4d37` W4.

---

## 1. Close-out table

| phase | verdict | evidence |
|---|---|---|
| **STEP 0** stop BRIEF A | **DONE** | `6dc5252`. O5 not run. Tree clean, pushed. The committed-ready work was the three reported picks defects. |
| **W1** withdraw calls | **DONE** | `c31c9fd`. Table dropped forward, both routes, `calls.py`, rail block, row block, tile marks, third forecaster, digest line, four plantings, `test_calls.py`. Orphan scan clean with **zero allowlist additions**. |
| **W2** palette + colour law | **DONE** | `948e094`. Ground darkened to R2's four values, `--green`/`--red` renamed `--win`/`--loss`, interactive is chrome, `audit.colour_law_faults` with two plantings, contrast re-run: every pair AA, worst 4.80:1. |
| **W3** picks as ruled | **DONE** | `bd7ac2f`. Two mono numbers, no gap, no graphs, Results rename with redirect, no resolved section, one breakpoint read by both JS and CSS. |
| **W4** qa | **DONE** | `67b4d37`. Suite green in one process, `verify.py` 4/4 EXIT=0, 103/103 plantings, read-cold pass on seven tiles. |

## 2. Deviations from the brief, stated up front

1. **The four-item nav (Picks · Record · Results · Settings) is NOT
   built.** R4 renames History to Results and W4's render list expects
   four nav items. The nav still carries seven: Picks · Record · Results
   · Schedule · Factors · Versions · Digest. **Killing Digest and
   Versions and folding Schedule into Settings is GRIDIRON_13 P5**,
   which is STEP 2 of this session's own order of work. Doing it here
   would have pulled a later brief's phase forward into this one.
   Reported rather than silently half-done.
2. **Results has no calendar yet.** W4's render list names "Results with
   the calendar". The calendar is GRIDIRON_13 P2 — again STEP 2. Results
   today is the History table under its new name.
3. **There is no login page render.** W4 names it; the login page is
   GRIDIRON_13 P6. STEP 2.
4. **The wordmark lost its green.** GRIDIRON_13 R8 said "the wordmark as
   is"; GRIDIRON_16 R2 says green means a pick won and nothing else, and
   the overnight brief states the mockup's green interactive elements are
   superseded by 16's tokens. Read the newer, more specific ruling as
   governing. One declaration to revert.

## 3. Rulings taken in your absence

| fork | ruling | why |
|---|---|---|
| Does the calibration chart keep green/red for in-band vs out-of-band buckets? | **No.** Encoded by emphasis: in-band `--muted`, out-of-band `--chrome`. | R2 is unqualified — a calibrated bucket is not a won pick. The new encoding also points the right way round: the old one made the unremarkable points the loudest thing on the chart. |
| Does the "at a glance" window bar count as a graph under R3? | **Yes, removed.** | R3 says no graphs anywhere on Picks. Each row already printed its count; the bar was a second, vaguer copy of the same number. |
| Do the factor contribution bars and the error box keep red? | **No.** | "Red only for a loss." A failed fetch and a pick that lost must not share a colour. The contribution track already draws a centre line, so position carries the sign. |
| Does `merged_forecaster_faults` keep its "informed forecaster must be labelled informed" rule? | **Withdrawn with the feature.** | No forecaster is informed any more, so the branch could only ever pass. Same reasoning as `call_stake_faults`: a guard that cannot fail is a guard on faith. |
| Does `db.WITHDRAWN` get to name `operator_calls`? | **Yes, skipped structurally.** | Naming it is *how* the table is dropped. It is the instrument of the removal, not a survival of it — so the scanner skips the drop list rather than exempting `db.py` wholesale. |
| How may a withdrawal drop a table without becoming a route around LAW 3? | **It may only drop what `schema.sql` no longer defines.** | Deleting the CREATE statement is the act that authorises the DROP. While `predictions` is still created there, no entry can touch it. The first version listed the record's tables by name and LAW 1's closure scan rejected the module in one run. |

## 4. Bugs I introduced, and how each was caught

| bug | caught by |
|---|---|
| `write_env` deleted `GRIDIRON_ACCESS_TOKEN` when writing the ntfy topic — the operator could not sign in | **by the operator**, reporting the app was down |
| `_PROTECTED` named market tables inside `db.py`, on every prediction path | **a test** — LAW 1's closure scan, first run |
| `tools/verify.py` still called `audit.check_a_call_is_not_a_stake` after W1 removed it | **the gate** — the suite could not see it |
| `renderForecasterPicker` still read `sc.operator_tier_table` | **a test** — the new `withdrawn_calls_faults` scan, first run |
| A finished slate rendered a blank Picks page | **by looking** at the branch after removing the resolved section |
| `const tierChoice` deleted with `resolvedRow`; blank Picks page | **a test** — fourteen browser tests, though it took six wrong probes to read the right error element |
| `rail_numbers_line` produced "a 12 points disagreement" | **by looking** at the output |
| A bulk rename turned the redirect test into a test that `#/results` redirects to `#/results` | **by looking** at the test after the rename |

Eight bugs; **five caught by tests or the gate, three by looking.** Better
than last session's ratio, and the two that mattered most — the LAW 1
violation and the surviving stump — were both caught by scans written in
the same session that broke the rule.

## 5. What is measurably true now

- **Suite:** green in one process, no skips, no xfail.
- **`verify.py`:** 4/4 PASS, EXIT=0.
- **Plantings:** 103/103 caught.
- **Orphan scan:** clean, zero allowlist additions (R1's condition).
- **Contrast on the darker ground:** every pair meets WCAG AA; worst
  4.80:1 (captions on a card, needs 4.5). The darker ground improved
  every ratio rather than costing any.
- **Read-cold, seven tiles:** every tile names the side the record
  stores.
- **Live database:** `operator_calls` dropped, 473 predictions intact.

## 6. What I would tell you if you were here

1. **The reported "empty rail on a daily sport" was never about the
   sport.** It was two mechanisms deciding one thing — `innerWidth` in
   the JS, a media query in the CSS — agreeing only while resize events
   arrived. When they disagreed the renderer built desk tiles the
   stylesheet would not style. Now both read the same `matchMedia`.
2. **`predict:nfl` ran twice on 2026-08-29** (05:55Z and 07:34Z) and
   wrote a full second set of week 1 forecasts. Both rows stay — LAW 3 —
   and the slate now shows the standing one and states how many it is
   hiding. **NFL week 1 calibration will count 26 questions twice.**
   That is a record problem, not a display one, and it needs a ruling.
3. **The token loss was mine and it locked you out.** A parameter was
   added and the one line that had to change with it was not. Two tests
   now cover it.
4. **STEP 2 is where the four-item nav, the calendar and the login page
   live.** Three of W4's render targets are in that brief, not this one.

## 7. Operator's verdicts

**Strongest thing:**

**Weakest thing:**

**What to do next:**

## 8. FOLLOWUPS delta

Non-zero. Added:

- **NFL week 1 holds two prediction sets.** 104 written, 78 standing, 26
  superseded. Both count in calibration. Needs a ruling on whether a
  double run should be detectable at write time.
- **`dumbbell()` in `app.js` has no call sites** and is exported for
  tests only. Pre-existing; not touched under R3 because it is not on
  Picks.
- **Rotate the ntfy topic and the Anthropic API key.** Both appeared in a
  transcript.
- **The ntfy push is ready but unreceived** — the phone app is not set up.
  Carried from BRIEF A O5 item 3, which was stopped by STEP 0.
