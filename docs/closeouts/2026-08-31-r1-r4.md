# Session close-out — R1–R4

Brief: `docs/briefs/2026-08-31-r1-r4-record-and-plain-pages.md`
(saved first, per MENTOR §4a, in `5ba5d50`).

Commits: `54dbfd4` R1 · `d139999` R2 · `3e100b9` the side door ·
`7629c5b` R3 · `c212780` R4 · `47f519d` R4 read-aloud.

---

## 1. Close-out table

| phase | verdict | evidence |
|---|---|---|
| **R1** the tier table IS the record | **DONE** | `54dbfd4`. Built with STRONG appearing TWICE, not once — see §2. `calibration.tier_table`, `tier_verdict`; planted violation keeps the bands separate. |
| **R2** reasons in three sentences | **DONE** | `d139999`. `WHY_MAX_SENTENCES = 3`; lead / second / market clause; two renderings per template chosen by sign at compose time. |
| — ruling: the side class fix | **DONE** | `3e100b9`. `language.side_named` as the one door, `audit.check_side_named`, two plantings. Both plantings kept, per the operator. |
| — ruling: size words folded into the lead | **DONE** | `3e100b9`. "Mostly it comes down to X." / "Pulling the other way, a little: Y." No trailing tags. |
| — ruling: city form in prose, full name in headings | **DONE** | `7629c5b`. `teams.location` fetched from the feed; `side_named(item, form=...)`. Verified: heading "Why Los Angeles Chargers", prose "the market has Los Angeles at 70%". |
| **R3** the rest of the pages | **DONE** | `7629c5b` + `c212780`. Factors plain name first; Versions "what changed"; History tier chips; Schedule task names; Digest three-sentence reasons; no bare dashes (`ABSENT = "not recorded"`, already in place). |
| **R4** look, then verify | **DONE** | `c212780`, `47f519d`. 28 route renders at 1100/390 plus the expanded pick; 642 passed; 58/58 plantings; verify.py 4/4. |

## 2. Deviations from the brief, stated up front

1. **R1's premise was false and the phase was built differently.** The
   brief called the buckets and the tiers "the same partition". There
   are FOUR buckets and THREE tiers, because STRONG spans 70–80% AND
   80%+. A single STRONG row is the merge LAW 4 forbids, and it
   flatters in a knowable direction. Built as two rows, each labelled
   with its band, with a planted violation keeping it that way.
2. **R2's "two renderings per template" was taken, not the fallback.**
   The brief offered composer-grammar-only if two renderings risked the
   sign flip. The sign is read at compose time from the fitted
   contribution and the template asserts nothing, so the risk the
   fallback existed for is not present.
3. **R4's "one expanded pick" was rendered for NFL, not MLB.** MLB
   could not supply one: tonight's slate is fully answered and six
   questions sat below the 70% floor, so its Picks page is all resolved
   rows. Stated rather than substituted silently.

## 3. Bugs I introduced, and how each was caught

| bug | caught by |
|---|---|
| Imported the humaniser into `sports/nba.py`, pulling a market-naming module into the NBA prediction closure | **a test** — LAW 1's import-closure scan, within seconds |
| Same again via `resolve.py` | **a test** — same scan, after my first fix was incomplete |
| `_prose_prop_type(r, sport)` in `prediction_detail`, which has no local `sport` | **a test** — `test_api.py`, NameError |
| Cut the sport label from the header strip, making two empty-record sports render identically | **a test** — `test_the_header_record_follows_the_selected_sport` |
| `grid-column: 1 / -1` on `.settled-why`, whose parent is flex, not grid | **by looking** at the rule I had just written. Inert, not wrong-looking. |
| The render script set `#versions` where the app routes on `#/versions`, so 28 shots were all the Record page | **by looking** at a screenshot named for a page it was not showing |
| A planted-violation source built with a broken quote escape, so the planting escaped | **the planting itself** reported ESCAPED |

The two "by looking" rows are harness gaps by MENTOR §2.3 and are in §8.

## 4. Vacuous passes found, and the class fix for each

1. **The plain-words scan ran on data that had none of the defects.**
   `test_no_internal_vocabulary_reaches_the_reader` covers all seven
   routes in a real browser and was green while the live Picks page read
   "Sam Darnold passing_yards over 165.5" and the live Schedule page
   read "ran degraded: llm_unavailable:api_error (1)". The smoke fixture
   builds its record from the CURRENT code path, so it has no fs1-era
   NULL `prop_type`, no degraded run, and no set a sport was absent
   from. The scan was right; the data under it was clean.
   *Class fix:* three tests that construct those shapes directly instead
   of hoping a fixture reproduces them.
2. **A guard scoped to where the rule was written, not where it applies.**
   `check_side_named` scans `language.py`. Two composers outside it —
   `views._resolved_story` and the digest's `settledRow` — reproduced
   the wrong-side defect for the fourth and fifth time.
   *Class fix:* `check_side_named_everywhere` scans the package for a
   raw side field interpolated into a string; reading it into a dict is
   plumbing, into a sentence is prose. The JS instance is gone because
   the server composes that sentence now.
3. **Two shadowed definitions that no guard could see.**
   `calibration.scorecard` and `version_comparison` each existed twice;
   the earlier of each pair was the pre-LAW-6 version querying every
   sport at once. Python discards a shadowed definition, so
   `require_sport` never fired, and the orphan scan saw the name as
   reached (the live definition's callers make it look used). Found by
   editing one and watching the page not change.
   *Class fix:* `check_no_shadowed_definitions`, in verify.py step 2.
4. **My own render pass was the most vacuous thing here.** It reported
   "plain-words clean on every route" from 28 screenshots of one page.
   *Class fix:* the script asserts the view actually changed before it
   believes a result.

## 5. What remains unverified

- **The tier table's verdicts are unverified as claims.** MLB moneyline
  has 24 settled in one band; LAW 4's gate is 100 and the bucket gate is
  20. Every row but LEAN reads "unproven". Nothing here says the model is
  calibrated — it says what has been counted so far.
- **The city/full-name rule is unverified for NFL's four pre-relocation
  tricodes** (LA, OAK, SD, WAS). ESPN's current feed has no row for them,
  so they fall back to the tricode. 30 of 34.
- **`stat_suffix` is unverified against a prop whose subject legitimately
  ENDS in a market name.** No such player is in the record; if one
  appears, the recovery would strip a real part of the name.
- **Nothing about the model's accuracy was tested this session.** This
  was a language and rendering brief throughout.

## 6. Operator's attention list

1. **The five-instance side defect is the story of this session.** Fixed
   three times as an instance and twice as a class. Worth a ruling on
   whether prose composition should be structurally impossible outside
   `language.py` rather than merely scanned for.
2. **Two pre-LAW-6 functions survived in the file for days.** They could
   not run, but a reader editing them would have changed nothing and a
   refactor could have revived them. The guard exists now; the question
   is whether anything else has the same shape.
3. **Look at the Versions page.** It is the one genuinely new surface and
   its "what changed" lines are long. If they read as inventory rather
   than as sentences, the cut is `CHANGE_NAMES_SHOWN`.
4. **Six MLB questions sat below the 70% floor tonight and none were
   asked.** That is the floor working, but four consecutive days of it
   would mean the ladder is mis-set for this sport.

## 7. Operator's verdicts

**Strongest thing:**

**Weakest thing:**

**What to do next:**

## 8. FOLLOWUPS delta

Non-zero. Added this session:

- **2026-08-31 — A CSS property that does nothing is invisible.**
  `grid-column` on a flex child; `flex: none` on an item expected to
  shrink (twice now — `#sport-tabs`, then `.season-record`). Nothing
  fails; the layout is simply not what the rule says. Worth a scan for
  properties inert against their parent's display mode.
- **2026-08-31 — The render harness had no assertion.** It named files
  after routes it never visited. Fixed in the scratchpad script; the
  shipped browser tests do route correctly, but nothing checks that a
  screenshot shows what it claims.
- **2026-08-31 — Thirty-two NFL prop rows carry `prop_type = NULL`**
  (fs1, before 07:34 on 2026-08-29). Permanent under LAW 3, handled at
  the reading end. Any new consumer of `prop_type` must decide
  explicitly whether it is reading or querying.
- **2026-08-31 — NFL team names cover 30 of 34 tricodes** (carried
  forward from K0–K4; still true).
- **K3's entry is closed** by `d139999` and `3e100b9`.
