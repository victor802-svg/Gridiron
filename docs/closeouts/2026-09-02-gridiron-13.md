# Session close-out — GRIDIRON_13 (P1–P7)

Brief: `docs/briefs/2026-09-01-progress.md`, executed as STEP 2 of
`docs/briefs/2026-09-02-overnight.md`. Mockup present and balanced:
`docs/mockup/gridiron_pages.html`, 213 lines, 129 matched divs, ends
`</html>` — so Part Two ran rather than stopping.

Commits: `9fb9a9c` P1 · `f4640b7` P2 · `4812532` P3 · `91b5ade` P4 ·
`529fff4` P5 · `4e35f75` P6 · `bbbf56d` P7.

---

## 1. Close-out table

| phase | verdict | evidence |
|---|---|---|
| **P1** gates | **DONE** | `9fb9a9c`. One component for four counters; closest-verdict line per sport; pace refuses below a week of history; three plantings. |
| **P2** calendar | **DONE** | `f4640b7`. Squares tinted by the day's balance only; voids counted and neither; click filters the table; three plantings. |
| **P3** settings | **DONE** | `4812532`. Append-only table, closed editable list, three locks verified over HTTP, OS read-back; four plantings. |
| **P4** qa (part one) | **DONE** | `91b5ade`. Suite, gate, renders at 1440/1280/390, and the real settings round trip. |
| **P5** four pages | **DONE** | `529fff4`. Picks · Record · Results · Settings; five old routes redirect. |
| **P6** entry | **DONE** | `4e35f75`. Login glance, sliding sessions, sign out everywhere, the stale-server refusal, the dumbbell icon. |
| **P7** qa (part two) | **DONE** | `bbbf56d`. Factor cards with search; two nav plantings; 117/117; verify.py EXIT=0. |

## 2. Deviations from the brief, stated up front

1. **P2's operator-calls toggle does not exist, and cannot.** The brief
   says "If operator calls exist, a toggle switches between 'model' and
   'you (informed)'". Operator calls were withdrawn earlier the same
   session by GRIDIRON_16 R1, which this session's own order of work put
   first. The condition is false, so the toggle is correctly absent
   rather than skipped.
2. **P5's "History filter" for the digest's day view is the calendar.**
   The brief asks for a filter reading "resolved on <date>"; the
   calendar filters on `league_date` — when the game was *played* —
   because that is the date the square counted, and filtering on a
   different date than the one counted would show a different set of
   games than the square claimed. The caption says "played on Monday 31
   August" for that reason.
3. **The measurements table survived P5's move.** The brief describes
   cards; the table carries the dated LAW 2 declarations and the Brier
   arithmetic, which a card cannot hold and which nothing else records.
   It is behind a collapsed `details` under "The measurements in full".

## 3. Rulings taken in your absence

| fork | ruling | why |
|---|---|---|
| Does a losing calendar day get the loss colour? | **Yes** — `.down` joins `.up` as a verdict selector. | A day tinted red *is* a day on which more picks lost. That is the colour doing its one job, not an exception. A `.download-button` in `--loss` is still refused, and a test pins that. |
| May the sign-in screen carry the record at all? | **Yes, counts only** — the brief ruled it. | It says the appliance is alive before you type. Four things are refused by name: a prediction, a side, a team with a line, a probability — and a rate, because a percentage is the model's claim about something. |
| Does `/api/health` get a build stamp, given it must carry no data? | **Yes.** | A build identifier says which code is answering, the same class of thing as the version string already there. The leak rule is about data *about the record*, and both health tests were updated with that argument rather than around it. |
| What happens when the launcher cannot tell which build is running? | **Attach.** | Refusing on missing information would make the app unopenable for a reason nobody could act on. Only a *known* mismatch asks. |
| Where does the rung-window date live? | **`config.READ_WINDOWS`, dated.** | It existed only in a sentence in `MLB_PROPS.md` and a FOLLOWUPS heading, so the interface could not count it down. |
| May the plain-words scan gain an exclusion? | **One class, `code-literal`.** | For strings the operator must reproduce exactly: the rotate commands, and the factor set version stamped on every prediction. Paraphrasing either produces something that does not match. Positional, like the two exclusions beside it. |

## 4. Bugs I introduced, and how each was caught

| bug | caught by |
|---|---|
| The renderer composed gate names in the browser | **the gate** — `check_js_composes_no_prose` |
| The renderer composed the Results caption (`' on ' + date`) | **the gate** — same scan, next phase |
| `.toLowerCase()` in the factor search | **the gate** — same scan again |
| A factor's declared rationale put `short_week_diff` on a card | **a test** — the plain-words scan |
| The prop ladders printed their stored keys | **a test** — same scan |
| A finished slate rendered a blank Picks page | **by looking** at the branch after removing the section |
| A task that never ran showed no "last ran" line at all | **a test** — the daily-glance test it broke |
| The settings page made three subprocess calls per load | **a test**, which read it before Health rendered |
| The sign-in button was painted the colour of the page | **by looking** at a render; the suite was green |
| `_open_route` carried its own copy of the landing route | **a test** — after the landing page moved |
| Five tests navigated by assigning an unchanged hash | **the tests themselves**, timing out |
| The history count query lacked the `games` join | **by looking**, immediately, when the day filter threw |

Twelve bugs; **ten caught by tests or the gate, two by looking.** The
inverse of the college-football session's ratio, and the reason is that
this session added scans in the same phases that broke their rules.

**The gate caught three things the suite could not**, all of them the
same scan. That scan does not run in pytest.

## 5. What is measurably true now

- **Suite:** green in one process, no skips, no xfail.
- **`verify.py`:** 4/4 PASS, EXIT=0.
- **Plantings:** 117/117 caught (12 added across P1–P7).
- **Orphan scan:** clean.
- **Contrast:** every pair meets WCAG AA; worst 4.80:1.
- **Renders:** 1440, 1280 and 390 for Record, Results, Settings and
  Picks; the login page at 1280.
- **A real settings round trip:** `predict:mlb` moved 11:00 → 11:05 from
  the page, `schtasks /Query` confirmed 11:05 independently, moved back,
  both entries in recent changes.

## 6. What I would tell you if you were here

1. **Two disagreements between the app and the OS scheduler are real and
   on screen now.** `predict:nfl` is recorded as 09:00 and the scheduler
   holds 11:00; no college football task is installed at all. The page
   says so in words. Neither is something I should decide.
2. **NFL week 1 still holds two prediction sets** — 104 written, 78
   standing, 26 counted twice in calibration. Carried from the earlier
   close-out; it needs a ruling.
3. **The wordmark lost its green** under GRIDIRON_16 R2. One declaration
   to revert if you disagree.
4. **STEP 3 (MLB run line and totals) and STEP 4 (the CFB ladder) have
   not been started.**

## 7. Operator's verdicts

**Strongest thing:**

**Weakest thing:**

**What to do next:**

## 8. FOLLOWUPS delta

Non-zero. Added:

- **`predict:nfl` and the scheduler disagree** (09:00 recorded, 11:00
  held) and **no `predict:cfb` task is installed.** Both are stated on
  Settings; both need a decision about which is right.
- **The digest's own page is gone**; its data feeds the greeting and the
  Results calendar. If a day-by-day narrative is wanted back, it is a
  new page, not a restoration.
- **`dumbbell()` in `app.js` still has no call sites.** Carried.
