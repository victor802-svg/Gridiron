# Close-out — the audit

Brief: `docs/briefs/2026-09-05-audit.md`. Report: `docs/AUDIT-2026-09-05.md`
(findings ranked, a one-paragraph verdict at the top, decisions in §6,
rulings in §7).

**The verdict slots are empty on purpose**, per the brief: they are the
operator's to fill after reading the report.

---

## 1. Against the brief

| phase | verdict | evidence |
|---|---|---|
| FIRST ACT — save the brief, read the five documents and the last five close-outs, run `verify.py` verbatim | | `c626568`; `scratchpad/audit_verify_verbatim.txt` — 4 steps PASS, 48/0, 190/190 caught, 1063 passed / 0 skipped, exit 0 |
| A1 LAW 3 integrity, fingerprint, integrity/foreign-key checks | | ok / 0 / 765 rows; fingerprint `b15a9f6f…` recorded as the first baseline — none existed |
| A2 standing-forecast rules | | 69 superseded, all early→final, 0 same-pass; `resolved()` excludes; two page counts did not → fixed `8296242` |
| A3 time truth | | 0 unvoided rows after start, 0 snapshots before prediction; `league_date` = UTC day for NFL/CFB/UFC → NFL fixed `58ce370`, CFB/UFC a decision; horizon counted days → fixed `6c53c37` |
| A4 the appliance | | OS 11 vs declared 15; Refresh killed 03:00Z 2026-09-05 with no row → fixed `7e23847`; ordering held every other day since 08-31; Capture/Recalibrate/UFC never registered → installer `0e52280`; re-run is the operator's |
| A5 both forecasters | | LLM wrote nothing on mlb 160/161, nfl wk1, cfb 20260904 (key blocked then); ledger under cap |
| A6 notifications | | 14 `sent`, 0 `sending` |
| A7 secrets | | clean in history, docs and tracked files |
| B1 restore the ten most recent faults in a throwaway tree | | `scratchpad/b1_results.json`: nine restorations (two plantings share a fault), every one produced its named suite failure |
| B2 scanner self-proof, the `\b` lineage | | 45 literal patterns compile, 0 backspaces, 0 dead anchors; 6 dynamic patterns rely on the fixture self-check |
| B3 comment-vs-code | | 8 of 9 raw-text scanners fired on a comment → fixed `bc81b88`, planted both directions |
| B4 duplicated maps | | task lists ×4 → `0e52280`; `SPORT_LABELS` ×2 → `0c1024a`; per-sport partial maps listed in the report |
| B5 30 assertions read for vacuity | | 27 real; 3 weak, named; 0 skips reachable |
| B6 fixtures that make a surface untestable | | Picks empty for 4 of 5 sports, Results for all, 0 task runs, 0 notifications — the cause of every plain-words leak D found |
| B7 gate freshness | | refuses skipped/absent browser tier, streams, `.venv` |
| C1 docstring and comment claims | | 243 resolved, 54 unresolved, one real (`_PROTECTED`) → `0c1024a` |
| C2 METHODOLOGY vs code | | §1/§2/§3/§5/§6/§7 stale → `3910435` |
| C3 DECISIONS_MADE "enforced by a guard" | | every named guard exists, is planted or tested, and `check_no_market_is_hidden` runs in the gate |
| C4 FOLLOWUPS | | ten items closed / overtaken / re-measured, dated → `be8ddc3` |
| C5 rulings in absence, all sessions | | one table, report §5 |
| C6 README and Settings | | README/CLAUDE → `3910435`; Health lists 15 vs OS 11, cannot show CatchUp → lists fixed, CatchUp a decision |
| D1 ten cards cold at 1440 and 390, plain-words on every route incl. LLM view | | 52 captures; 10 cards read, every reading matched the stored side; 4 leaks → `66de8a7`, `d160f3d`; hero tag → `8c377fd` |
| D2 unproven surfaces on the phone | | visible without hover on every card face; CFB totals note not seen at 390 — recorded for the operator |
| D3 login page | | counts only; button 340×44 at 13.9:1 |
| D4 contrast, tap targets, overflow | | 0 below AA; 0 overflow at 390; 700px header band still 80px; fields and links → `425e693`, dots/tabs a decision |
| E1 fix only clear defects, one commit each with plantings | | 13 commits, 5 new plantings, 2 new gate checks, 24 new tests |
| E2 full suite under `.venv`, no skips; every planting; `verify.py` green; renders at both widths | | see the report §4 |
| E3 the report, ranked, verdict first; close-out with verdict slots empty; push | | `docs/AUDIT-2026-09-05.md`; this file; pushed |

## 2. Rulings taken in your absence

Report §7, ten of them. The two that would change something visible if you
disagree: the NFL loader now writes the league's own day (one refresh repairs
the rows), and lineless cards rank by confidence with a tag that says so.

## 3. Blocked, by design

Report §6, twelve decisions. The first is the one that matters today:
**re-run `tools/schedule_install.ps1`.**

## 4. What was not done

- No row of the record was edited, voided or resolved by hand.
- The installer was not run; the OS holds the same eleven tasks it held this
  morning.
- No reasoning pass was run to fill the slates answered while the key was
  blocked (answered-once).
- Phase D read a byte copy of the live database, so the live poll wrote nothing.
