# Close-out — GRIDIRON_TODAY (2026-09-07)

Against the brief saved at `docs/briefs/2026-09-07-today.md`. One row per phase.

| # | phase | verdict | evidence |
|---|---|---|---|
| FIRST ACT | Save the brief before executing it | **DONE** | `docs/briefs/2026-09-07-today.md` (43a13dd), with how it is read. |
| T1 | The Today screen | **DONE** | Two groups that never blend, at the top of the route the operator opens. CLEARS THE BAR carries fair value, the venue's price, the edge after fees, the size and the gate; WATCHING carries every other shortlisted question with its own edge in cents, including negative ones. Rendered at 1100 and 390 pixels. |
| T1 | The words | **DONE** | `audit.ADVICE_WORDS` gains "worth it" and "worth backing" beside the words it already refused; `plant_a_watched_row_called_worth_backing` puts one in a watched label and is caught by name. |
| T1 | The fee line | **DONE** | Under both groups, every day: the venue's fee at today's median price, in cents, so the cost of a no-edge bet sits on the same screen as the temptation. On a slate with no venue price it says that instead of inventing one. |
| T2 | "I took this" | **DONE** | `picks_taken` holds a prediction id and a timestamp. Append-only by trigger, refuses a pick taken before it was forecast, idempotent on a second tap. The route is the app's fourth POST and argues for itself in the test that counts them, with the session and CSRF locks the settings route has and its own writable handle. |
| T2 | What it may never become | **DONE** | `audit.check_taken_is_not_a_ledger` fails by name on a money-shaped column; `audit.check_taken_not_in_training` refuses the table's name in any of eleven modules that train or correct. Both proved by plantings rather than asserted. |
| T2 | The comparison it exists for | **DONE** | `calibration.taken_comparison`: three curves, taken, passed over and every forecast, never merged, behind the same gate. It reads a person's choices and nothing that trains the model reads it. |
| T3 | Make the learning visible | **DONE** | A Record panel, one row per category: where the correction stands, what it does to a seventy-per-cent claim in words, the drift verdict or its count, and the date the refit last ran. The machinery is untouched. |
| T4 | What the close-out must state | **DONE** | Below. |

## What the panel found the moment it existed

**Three baseball categories are eligible and have never been fitted.**
Moneyline has 110 settled rows, the point spread 52, the total 52 — all past
the fifty a correction needs — and every stored correction row has
`n_train = 0`, which is a placeholder saying there was nothing to fit when the
refit last ran. That was **1 September**.

The Windows task that would fix it, `Gridiron-Recalibrate`, has never fired:
its last run time reads 30 November 1999 and its result code is "has not yet
run". Its trigger begins 5 September and the next firing is due at 06:00 local
on 7 September, so this resolves itself within hours — but nothing said so
until the panel existed, and the operator's belief that the app does not learn
was a reasonable reading of a page that never mentioned it.

**The first version of that sentence was a contradiction.** It read "not yet
fitted: 52 of 50 settled rows, 0 more before a correction is calculated". A
placeholder is not a fit, and the sentence now distinguishes four states and
names the date the refit last ran.

## T4's required statements

**Days since the observation window opened: one.** It began on 2026-09-07 and
this is that day. **Days with an empty CLEARS THE BAR group: one of one.**
Nothing has cleared the venue's fee on any slate yet, and on the current record
nothing can: the recommendation engine prices against the venue's own quote
through an at-the-line claim, and no stored prediction carries the frozen
distribution a claim needs. The first slate run writes both.

That fraction — 1 of 1 — is not yet evidence of anything. It is recorded
because the brief asks for it from the first day, and it is the number to watch
on 21 September: if the bar is cleared on few days out of fourteen, that is the
honest headline about how often a real edge appears, and it belongs in
`FOLLOWUPS.md` in those words rather than being smoothed over.

**No training or correction query reads the taken table, proven rather than
asserted.** `plant_the_taken_table_in_a_training_query` writes a training query
that joins `picks_taken` into `predictions` and `audit.taken_in_training_faults`
names the module. The scan covers eleven modules: the model's baseline,
logistic, predict and questions; correction and drift; the ranker; and the four
factor modules.

## Rulings taken in the operator's absence

1. **Today leads the Picks route rather than becoming a fifth page.**
   `audit.nav_faults` rules the nav at four pages and a planting proves it. A
   layout does not get to break a law, and Picks is the route he opens daily.
2. **The older "Worth taking" panel is retired**, with its reason left in the
   markup. It said "nothing priced wrong enough today" directly beneath Today
   saying "nothing clears the venue's fee today"; two panels reporting one fact
   is how a page starts disagreeing with itself.
3. **A taken pick cannot be untaken.** The table is append-only like everything
   else here, so a mis-tap is permanent. Recorded as a follow-up rather than
   solved: the fix is either a second append-only row saying it was undone, or
   nothing, and that is the operator's call.
