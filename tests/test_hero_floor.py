"""RETIRED WITH THE HERO (GRIDIRON_THREE_STATES, 2026-09-08).

This file drove `hero_floor`: that the hero showed no claim below the declared
confidence floor, that a tab with no qualifying pick showed a sentence instead
of promoting one, and that the shipped slate never led with an unqualified
claim.

THERE IS NO LEAD. Every card on Upcoming is the same size, and which group a
card sits in is decided by the bar -- an edge after fees, and a return on the
stake -- rather than by a confidence sort. What the floor protected is
protected more widely now: a card below its market's gate says "no measured
edge" on its own face, whatever group it is in, which binds every card rather
than the one at the top.

The file is kept, empty of tests, so the retirement is visible where the tests
were rather than only in a close-out.
"""
