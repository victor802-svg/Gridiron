"""RETIRED WITH THE VIEW MENU (GRIDIRON_THREE_STATES, 2026-09-08).

This file drove the menu that chose a forecaster and a pass: that it was a
44-pixel tap target, that it opened and closed, that Escape closed it and
focus came back to the button.

THE MENU IS GONE. It was one of three segmented controls standing above the
first card, and the page now names the forecaster it is counting in the day
strip instead of asking the reader to choose one. The choice itself still
exists in `viewChoice` and is still read; nothing on the page sets it, which
is the point.

Kept, empty of tests, so the retirement is visible where the tests were.
"""
