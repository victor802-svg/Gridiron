[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    slow: hits the network or runs a full backtest
    browser: requires playwright + a chromium download
filterwarnings =
    ignore::DeprecationWarning
